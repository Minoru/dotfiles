<?xml version="1.0"?>
<!DOCTYPE fontconfig SYSTEM "fonts.dtd">
<!-- $HOME/.fonts.conf for per-user font configuration -->
<fontconfig>

<!--
	Private font directory
-->
<dir prefix="default">.fonts</dir>

</fontconfig>
